# Upcoming Features: Visual Analytics (Crypto)

## 1. Portfolio Allocation Donut Chart
**Description:** A sleek, interactive donut chart on the Crypto dashboard that visually breaks down the percentage allocation of each holding (e.g., 45% BTC, 30% ETH, 25% USDT).
**Tech Stack:**
- Frontend: `recharts` or `chart.js` for React.
- State: Loop through current `holdings` data to calculate percentages dynamically based on the current live price * quantity.
**Implementation Steps:**
1. `npm install recharts` in the frontend.
2. Create a `<PortfolioDonut />` component.
3. Pass `holdings` data inside, map it to `{ name: 'BTC', value: 45000 }` format.
4. Style it with custom neon/dark-theme colors for the PieChart segments to match the Vercel theme.

## 2. 7-Day Price Sparklines
**Description:** Mini, glowing trend-line graphs next to each coin in the holding table. Green if the 7-day trend is upward, red if downward.
**Tech Stack:**
- Frontend: `recharts` `<LineChart />` (without X/Y axes or grid lines).
- Backend (CoinGecko): Fetch the 7-day sparkline data array from CoinGecko.
**Implementation Steps:**
1. Update Go backend `crypto_service` to request `sparkline=true` when querying CoinGecko prices.
2. Pass the array of prices (last 7 days/168 hours) to the frontend.
3. In `Crypto.jsx`, render a mini `LineChart` inside the table row.
4. Set the line `stroke` color conditionally: `prices[last] > prices[first] ? '#34d399' : '#f87171'`.

## 3. Dark Mode / Theme Toggle
**Description:** A simple switch in the navbar to swap between the sleek dark theme and a clean light theme.
**Tech Stack:**
- Frontend: React `Context` or saving preference to `localStorage`.
- CSS: Swapping out the CSS variable roots mapping (`--bg-primary`, `--text-primary`, etc.).

---
*Generated by LendTrack Assistant, reserved for future implementation!*

## 4. CSV Import (Loan Data Upload)

**Description:** An "📤 Import CSV" button on the Person Detail page (next to Export CSV) that lets the user upload a previously-exported CSV file to bulk-create loans and transactions for that person — no manual data entry needed.

**How it works (No backend changes required):**
- A hidden `<input type="file" accept=".csv">` is triggered when the button is clicked.
- The browser reads and parses the CSV file entirely on the frontend.
- For each row, the frontend calls the **existing** API endpoints sequentially:
  - `POST /api/v1/loans` → creates the loan
  - `POST /api/v1/loans/{id}/transactions` → adds each installment
- A progress indicator / success toast is shown after all rows are processed.

**Placement Options:**
- **Option A** — Person Detail page only (imports for that specific person)
- **Option B** — People page (global import — can create new people + their loans from one CSV)

**CSV Format:** Same format produced by the existing Export CSV button — so Export → Edit → Re-Import works seamlessly.

**Tech Stack:**
- Frontend only (`FileReader` API + existing `apiFetch` helper)
- No new backend endpoints needed for Option A
- Zero new npm packages required

**Implementation Steps:**
1. Add a hidden `<input type="file" accept=".csv" ref={fileInputRef}>` in `PersonDetail.jsx`
2. Add `📤 Import CSV` button that calls `fileInputRef.current.click()`
3. In the `onChange` handler, use `FileReader` to read the file text
4. Parse CSV rows (split by newline + comma), skip header rows
5. Loop through data rows → call `addLoan` + `addTransaction` for each
6. Show toast/alert on completion with count of records imported

